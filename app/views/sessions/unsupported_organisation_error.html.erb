<%= content_for :title, I18n.t("errors.sign_in.unsupported_organisation.page_title") %>

<h1 class="govuk-heading-l"><%= I18n.t("errors.sign_in.unsupported_organisation.page_title") %></h1>
<p class="govuk-body"><%= I18n.t("errors.sign_in.unsupported_organisation.page_body.supported_schools") %></p>
<% I18n.t("errors.sign_in.unsupported_organisation.page_body.supported_schools_list").each do |item| %>
  <ul class="govuk-list govuk-list--bullet"><%= item %></p>
<% end %>
<% I18n.t("errors.sign_in.unsupported_organisation.page_body.paragraphs").each do |paragraph| %>
  <p class="govuk-body"><%= paragraph %></p>
<% end %>
