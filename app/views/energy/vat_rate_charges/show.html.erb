<%= content_for :title, "#{@form.errors.any? ? 'Error: ' : ''}#{I18n.t("energy.vat_rate_charge.heading")} - #{I18n.t('energy.service_name')}" %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <p class="govuk-caption-l"><%= I18n.t("energy.vat_rate_charge.title")%></p>

    <%= render "form" %>
  </div>
</div>