<%= content_for :title, "#{@electricity_meter_detail.errors.any? ? 'Error: ' : ''}#{I18n.t("energy.electricity_meter.heading")} - #{I18n.t('energy.service_name')}" %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render "form" %>
  </div>
</div>
