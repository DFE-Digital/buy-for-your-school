<dd class="govuk-summary-list__value">
  This content is mandatory and will be added to your specification.
</dd>
<dd class="govuk-summary-list__actions">
  <% if step.acknowledged? %>
    <%= link_to "Review", journey_step_path(@journey, step), class: "govuk-link", id: step.contentful_type %>
  <% else %>
    <%= link_to "Confirm", journey_step_path(@journey, step), class: "govuk-link", id: step.contentful_type %>
  <% end %>
</dd>