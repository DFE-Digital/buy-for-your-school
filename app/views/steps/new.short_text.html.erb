<%= render layout: "steps/form_wrapper" do |f| %>
  <%= f.govuk_text_field :response,
    label: { text: @step.title, size: 'xl' },
    hint: { text: @step.help_text },
    width: "one-third"
  %>
<% end %>
