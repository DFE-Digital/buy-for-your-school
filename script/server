#!/bin/sh
#
# Docker development environment Rails server
#
#

set -e
echo "NB: run script/build if the image ghbs:dev does not exist locally"
echo "-----------------------------------------------------------------"

docker-compose -f docker-compose.yml -f docker-compose.dev.yml --project-name ghbs up --detach

docker attach ghbs_dev

