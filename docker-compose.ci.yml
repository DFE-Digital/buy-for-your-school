version: "3.8"
services:
  app:
    build:
      cache_from:
        - app_test:latest
    image: app_test
    environment:
      CC_TEST_REPORTER_ID: ${CC_TEST_REPORTER_ID}
      GIT_BRANCH: ${GIT_BRANCH}
      GIT_COMMIT_SHA: ${GIT_COMMIT_SHA}
